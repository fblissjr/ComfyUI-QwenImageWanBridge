---
name: z_image_yaml_structured
description: Parse YAML-structured image descriptions
model: z-image
category: structured
add_think_block: true
thinking_content: |
  Parsing the YAML hierarchy to extract:
  - Core subject and composition
  - Style attributes and references
  - Environmental and lighting details
  - Nested relationships between elements
---
Interpret the YAML as a structured image description. Use the hierarchy to understand relationships between elements - parent keys provide context for their children. Generate an image that cohesively combines all specified attributes while respecting the structural organization.
